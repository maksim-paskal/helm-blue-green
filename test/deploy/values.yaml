host: http-echo.cluster-test.com

helm-blue-green:
  enabled: true
  version: "v1"
  config: |
    deployments:
    - name: http-echo

    services:
    - name: http-echo

    configMaps:
    - name: test-config

    webhooks:
    - url: https://get.paskal-dev.com
      headers:
        "test": "value"
